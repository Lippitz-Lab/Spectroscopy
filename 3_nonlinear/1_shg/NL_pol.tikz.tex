% \documentclass[margin=0mm]{standalone}
% \usepackage{tikz}
% \usepackage{pgfplots}
% \pgfplotsset{compat=newest}


% \usepackage{currfile,hyperxmp}
% \usetikzlibrary{math,matrix,fit,positioning}
% \usepgfplotslibrary{groupplots}


% \begin{document}

 

\begin{tikzpicture}[font=\small]
%\useasboundingbox (-1.0,-1.0) rectangle (10.2,4.2);
%\draw (-1,-1.0) rectangle (10.2,4.2);
%


\pgfmathsetmacro\plotw{30mm} % width of subplots
\pgfmathsetmacro\ploth{32mm} % height of smaller subplots
\pgfmathsetmacro\vsep{15pt}  % vertical separation between subplots
\pgfmathsetmacro\hsep{15pt}  % horizontal separation between subplots

\begin{groupplot}[
    group style={
       group size=3 by 1,
       vertical sep=\vsep,
       horizontal sep=\hsep,
       x descriptions at=edge bottom},
    height=\ploth,
    width=\plotw,
  %  ymax = 5,
    ymin = 0,
    xtick pos = bottom,
    scale only axis,
      x tick label style={/pgf/number format/.cd,%
          scaled x ticks = false,
          set thousands separator={},
          fixed},
    ]
    



\nextgroupplot[ytick = \empty, yticklabel = \empty, xtick = \empty, xticklabel = \empty,
ylabel = {polarization}, xlabel = {field},
%ytick pos = left, ytick = {0}, 
 xmin=-1, xmax = 1,
 ymin = -1, ymax = 1,
 axis lines=center,
 x label style={anchor=north east},
 y label style={anchor=east},
 ]
 \addplot[no markers, black,thick, domain = (-1:1)]  {x};
 \addplot[no markers, blue,thick, domain = (-1:1)]  {x - 0.4 * x^2};

 \node at (0.8, 0.3)[left] {NL};
 \node at (0.85,0.9)[left] {lin};

	 	

\nextgroupplot[ytick = \empty, yticklabel = \empty, xtick = \empty, xticklabel = \empty,
ylabel = {}, xlabel = {time},
%ytick pos = left, ytick = {0}, 
 xmin=-50, xmax = 800,
 ymin = -1.3, ymax = 1.3,
 axis x line=center,
 axis y line=none,
 %x label style={anchor=north},
 %y label style={anchor=east},
 ]
 \addplot[no markers, thin, dashed, domain = (0:720), samples=300]  {sin(x)};
 \addplot[no markers, black, thick, domain = (0:720), samples=300]  {0.8 * sin(x)};

 \node at ( rel axis cs: 0.5, 0.95) {linear polarization};

	
\nextgroupplot[ytick = \empty, yticklabel = \empty, xtick = \empty, xticklabel = \empty,
ylabel = {}, xlabel = {time},
%ytick pos = left, ytick = {0}, 
 xmin=-50, xmax = 800,
 ymin = -1.3, ymax = 1.3,
 axis x line=center,
 axis y line=none,
 %x label style={anchor=north},
 %y label style={anchor=east},
 ]
 \addplot[no markers, thin, dashed, domain = (0:720), samples=300]  {sin(x)};
 \addplot[no markers, blue, thick, domain = (0:720), samples=300]  {0.8 * sin(x) - 0.4 * (0.8 * sin(x))^2 };

 \node at ( rel axis cs: 0.5, 0.95) {nonlinear polarization};

	

 
 
 
\end{groupplot}


\end{tikzpicture}

%\end{document}