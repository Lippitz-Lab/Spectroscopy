% \documentclass[margin=0mm]{standalone}
% \usepackage{tikz}
% \usepackage{pgfplots}
% \pgfplotsset{compat=newest}


% \usepackage{currfile,hyperxmp}
% \usetikzlibrary{math,matrix,fit,positioning}
% \usepgfplotslibrary{groupplots}


% \begin{document}

 

\begin{tikzpicture}[font=\small]
%\useasboundingbox (-1.0,-1.0) rectangle (10.2,4.2);
%\draw (-1,-1.0) rectangle (10.2,4.2);
%


\pgfmathsetmacro\plotw{100mm} % width of subplots
\pgfmathsetmacro\ploth{35mm} % height of smaller subplots
\pgfmathsetmacro\vsep{5pt}  % vertical separation between subplots
\pgfmathsetmacro\hsep{15pt}  % horizontal separation between subplots

\begin{groupplot}[
    group style={
       group size=1 by 2,
       vertical sep=\vsep,
       horizontal sep=\hsep,
       x descriptions at=edge bottom},
    height=\ploth,
    width=\plotw,
  %  ymax = 5,
    ymin = 0,
    xtick pos = bottom,
    scale only axis,
      x tick label style={/pgf/number format/.cd,%
          scaled x ticks = false,
          set thousands separator={},
          fixed},
    ]
    



\nextgroupplot[ytick = \empty, yticklabel = \empty, xtick = \empty, xticklabel = \empty,
ylabel = {NL polarization}, xlabel = {position},
%ytick pos = left, ytick = {0}, 
 xmin=-0.4, xmax = 2.5,
 ymin = 0, ymax = 1.3,
 height=20mm,
 axis y line=left,
 axis x line=bottom,
 x label style={anchor=north east, at={(axis description cs:1,-0.03)}},
 %y label style={ rotate=90, anchor=south east}
 ]
 \addplot[no markers, black,thick, domain = (-0.4:2.5), samples=1000]  {cos(x * 360)^2 };
 
\draw[<->] (0, 1.1) -- node[above] {$n_1 \lambda_1$ } (1, 1.1);	 	
\draw[fill] (0,1) circle (2pt);
\draw[fill, gray] (1,1) circle (2pt);

\nextgroupplot[ytick = \empty, yticklabel = \empty, xtick = \empty, xticklabel = \empty,
ylabel = {SH field}, xlabel = {position},
%ytick pos = left, ytick = {0}, 
xmin=-0.4, xmax = 2.5,
ymin = -4, ymax = 2.2,
 axis y line=left,
 axis x line=none,
 x label style={anchor=north east},
% y label style={ rotate=90, anchor=south east}
 ]
 \addplot[no markers, black,thick, domain = (0:2.5), samples=1000]  {cos(2 * x * 360) };
 
\draw[<->] (0, 1.3) -- node[above] {$2 n_2 \lambda_2$ }  (1, 1.3);	 	
\node[left] at (0, 0) {$n_1 = n_2$};
\draw[fill] (0,1) circle (2pt);
\draw[fill, gray] (1,1) circle (2pt);

\addplot[no markers, black,thick, domain = (0:2.5), samples=1000]  {-3 + cos(1.7 * x * 360) };
\addplot[no markers, black,thick, domain = (1:2.5), samples=1000]  {-3 + cos(1.7 * (x-1) * 360) };

\draw[<->] (0, -1.7) -- node[above] {$2 n_2 \lambda_2$ }  (1 * 2 / 1.7, -1.7);	 	
\node[left] at (0, -3) {$n_1 < n_2$};
\draw[fill] (0,-2) circle (2pt);
\draw[fill, gray] (1,-2) circle (2pt);
 
\end{groupplot}


\end{tikzpicture}

%\end{document}